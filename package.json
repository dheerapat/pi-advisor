{
  "name": "pi-advisor",
  "version": "0.1.0",
  "description": "Delegate hard problems to a more capable advisor model for guidance",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "pi-package"
  ],
  "type": "module",
  "pi": {
    "extensions": [
      "./extensions/advisor/index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
